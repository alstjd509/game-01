# game-01 — 웹 미니게임 1호 작업 폴더

> **이 문서는 PC·세션이 바뀌어도 Claude에게 그대로 전달되는 인수인계 문서다.**
> 이 폴더에서 작업하는 Claude는 이 문서만으로 상태를 파악하고 이어서 작업한다.
> **작업 세션을 마칠 때마다 Claude가 §1 "현재 상태"를 직접 갱신한다.** 큰 결정은 근거와 함께 §1 결정 이력에 남긴다.

## 1. 현재 상태

**기준일: 2026-09-01**

- 단계: **컨셉 선정 전** (파이프라인 §4의 0단계)
- 게임명: 미정
- 컨셉 후보 6개 중 미선정 — ①스네이크 서바이버(먹은 게 몸 파츠=무기가 되는 스네이크×뱀서) ②원버튼 미니뱀서(마우스 추적 이동만, 3분 압축 뱀서) ③머지 디펜스(수박게임식 합치기로 유닛 생성→웨이브 방어) ④딥 다이버(연료 소진 전 귀환하는 채굴 런+장비 업글) ⑤탄막 회피 러너(회피만, 생존 시간=재화) ⑥궤도 슈터(클릭 하나로 공전·사격)
- 다음 할 일: ① 컨셉 1개 선정 → ② `docs\01_기획.md` 작성(1~2페이지) → ③ Phaser+Vite 스캐폴딩(이 폴더) → ④ 그레이박스 프로토

### 결정 이력
- 2026-09-01: 프로젝트 방향 확정(§2·§3 내용). 폴더 생성.
- 2026-09-01: 논의 이력 git 관리를 위해 방향 정본 `docs\00_프로젝트_방향.md`를 이 폴더로 이동, 저장소 초기 구성(.gitignore·README).

## 2. 프로젝트 정체성 (변하지 않는 사실)

- 목표: **중독성·재미 위주 웹 미니게임을 짧은 공수로 출시까지.** 수익은 작아도 실제로 시도한다.
- 출시: **itch.io 먼저**(HTML5 업로드) → 반응 좋으면 Poki·CrazyGames 등 포털 제출(광고 수익 셰어), 모바일 포팅 검토.
- 사용자의 기존 Unity/스팀 프로젝트(다른 PC, Codex로 개발)와 **완전히 별개의 트랙**이다. 웹에서 여러 개 만들며 설계·디자인·게임성 감각을 익히는 것이 1차 목적.
- 규모: 1작품 1~2개월, 본업 병행(주당 시간 변동 큼). **초소형은 지양** — 런 간 메타 성장 정도의 볼륨은 포함한다.
- 장르 축: **아케이드 "한판 더" × 뱀서류·로그라이트.** 한 판 1~5분, 죽으면 즉시 재도전, 죽음의 원인이 "내 탓"으로 느껴져야 한다.
- 아트: 프로토 단계는 도형(그레이박스)만. 이후 도트(Aseprite)·무료 에셋(Kenney.nl 등 CC0 우선)·AI는 컨셉 목업용.

## 3. 기술 지침

- 스택: **Phaser 3(안정판) + Vite + vanilla JS.** TypeScript·React 등 추가 프레임워크는 쓰지 않는다(반복 속도 우선).
- 이 폴더가 코드 루트. 기획·회고 문서는 `docs\` 하위에 둔다.
- 조작: 마우스(또는 단순 키 1~2개) 위주 — 웹 플레이와 향후 모바일 포팅을 고려.
- 배포: `vite build` 결과물 zip → itch.io HTML5 게임 등록.
- 사용 에셋은 `docs\에셋_라이선스.md`에 출처·라이선스를 누적 기록한다(CC0 우선, CC-BY는 크레딧 표기).

## 4. 작업 원칙 (Claude 지침)

1. 파이프라인: 컨셉 선정 → 미니 기획서(1~2p) → **그레이박스 프로토** → 재미 판정 → 폴리싱(아트·사운드·손맛) → itch.io 출시 → 회고.
2. **문서보다 손맛.** 기획서를 길게 쓰지 말고 빨리 플레이 가능한 상태로 만들어 굴려본다.
3. 재미 판정에서 "한 판 더 하고 싶지 않다"면 **미련 없이 폐기**하고 다음 컨셉으로 간다(매몰비용 무시).
4. 사용자에게 보이는 모든 텍스트는 **한국어**로 쓴다.
5. 세션을 마치면 이 파일 §1(현재 상태·결정 이력)을 갱신해 다음 세션이 이어받을 수 있게 한다.
6. 상위 방향 정본 = `docs\00_프로젝트_방향.md` (이 저장소에 포함 — 컨셉 후보 상세, 무료 에셋·툴 목록, 선정 기준 5개). 컨셉·방향 논의 전에 함께 읽는다.
